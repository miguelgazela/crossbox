<%= render "./navbar" %>


  <!-- <% if @trainings.length > 0 %>

    <div class="card">

      <h1 class="page-header hidden-xs">Meus Treinos</h1>
      <h3 class="page-header hidden-sm hidden-md hidden-lg text-center">Meus Treinos</h3>

      <table class="table">

          <thead>
            <tr>
              <th>#</th>
              <th>Data</th>
              <th>Estado</th>
            </tr>
          </thead>

          <tbody>

            <% @trainings.each_with_index do |dict, index| %>

              <tr>
                <td><%= index + 1 %></td>
                <td><a href="/workouts/<%= dict[:training].workout.id %>"><%= dict[:training].workout.date.strftime("%d/%m %H:%M") %></a></td>
                <td><%= dict[:state] %></td>
              </tr>

            <% end %>

          </tbody>

      </table>

    </div>

  <% end %> -->


<section>

  <h2>TREINOS</h2>

  <ul class="list-group class-list">

  </ul>

</section>

<!-- <div id="timetable" class="card">

  <h1 class="page-header hidden-xs">Horário de Aulas
    <div class="pull-right">
      <% if current_user.role == 'admin' || current_user.role == 'coach' %>
        <button class="btn btn-default main-header-calendar-btn" type="button" id="go-week-before"><%= fa_icon "angle-double-left" %></button>
        <button class="btn btn-default main-header-calendar-btn" type="button" id="go-today"><%= fa_icon "calendar-check-o" %> Ir para hoje</button>
        <button class="btn btn-default main-header-calendar-btn" type="button" id="go-week-after"><%= fa_icon "angle-double-right" %></button>
        <button class="btn btn-default" type="button" onclick="redirectToAddWorkouts()"><%= fa_icon "calendar-plus-o" %> Adicionar Aulas</button>
      <% end %>
    </div>
  </h1>

  <h3 class="page-header hidden-sm hidden-md hidden-lg text-center">Horário de Aulas</h3>
  <% if current_user.role == 'admin' || current_user.role == 'coach' %>
  <div style="text-align: center;">
  <div class="btn-group hidden-sm hidden-md hidden-lg" role="group" aria-label="...">
    <button type="button" class="btn btn-default" id="go-week-before"><%= fa_icon "angle-double-left" %></button>
    <button type="button" class="btn btn-default" id="go-today"><%= fa_icon "calendar-check-o" %> Hoje</button>
    <button type="button" class="btn btn-default" id="go-week-after"><%= fa_icon "angle-double-right" %></button>
  </div>
  <button class="btn btn-default hidden-sm hidden-md hidden-lg" type="button" onclick="redirectToAddWorkouts()"><%= fa_icon "calendar-plus-o" %> Adicionar Aulas</button>
  </div>
  <% end %>

</div> -->

<script id="class-day-template" type="text/x-jsrender">

<li class="list-group-item class-day" data-class-day="{{:workoutDate}}">

  <div class="class-day-column class-day-column-date">
    <p {{if workoutWeekDay == 'Hoje'}}class="class-day-column-date-today"{{/if}}>{{:workoutDay}}</p>
    <p>{{:workoutMonth}}</p>
  </div>

  <div class="class-day-column class-day-column-description">
    <p>{{:workoutWeekDay}}</p>
    <p>{{:workoutsAvailableDescription}}</p>
  </div>

  <span class="glyphicon glyphicon-chevron-down" aria-hidden="true"></span>

</li>

<ul class="list-group class-hour-list"></ul>

</script>


<script id="class-hour-template" type="text/x-jsrender">

<li class="list-group-item class-hour-list-item {{:hiddenClass}}" data-class-hour="{{:workoutDateHour}}">

  <div class="class-hour-column">
    <p class="class-hour-time {{if inWorkout}}class-hour-time-active{{/if}}">{{:workoutHour}}</p>
  </div>

  <div class="class-hour-column class-hour-column-info">

    <p class="class-hour-title {{if inWorkout}}class-hour-title-active{{/if}}">Treino das {{:workoutHour}}</p>

    <div class="class-hour-elements">
      <span>{{:availableSpots}} vagas</span>
      {{for thumbnails}}
        <img src="{{:src}}" alt="" class="img-circle"/>
      {{/for}}
      {{if excessUsers}}
        <span>+{{:excessUsers}}</span>
      {{/if}}
    </div>

    <div class="class-hour-controls">
      <a class="btn btn-sm btn-default" href="/workouts/{{:workoutID}}" role="button"><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> Ver</a>

      {{if !inWorkout}}
      <button type="button" class="btn btn-sm btn-default" onclick="enterWorkout({{:workoutID}})"><span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span> Inscrever</a>
      {{else}}
      <a class="btn btn-sm btn-default btn-forfeit" href="/workouts/{{:workoutID}}/state?a=leave&rh=1" role="button"><span class="glyphicon glyphicon-minus-sign" aria-hidden="true"></span> Desistir</a>
      {{/if}}

    </div>

  </div>

  <div class="class-hour-graphical-availability {{:availabilityClass}}" data-completion="{{:completion}}">
    <div>{{:availableSpots}}</div>
  </div>

</li>


</script>
