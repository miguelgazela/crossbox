<%= stylesheet_link_tag "sessions", :media => "all" %>

<div class="site-wrapper">

  <div class="site-wrapper-inner">

    <div class="cover-container">

      <div class="masthead clearfix">
        <div class="inner">
          <h3 class="masthead-brand">SLNutrition CrossBox</h3>
          <nav>
            <ul class="nav masthead-nav">
              <li class="active"><a href="#">Início</a></li>
              <li><a href="/contacts">Contactos</a></li>
            </ul>
          </nav>
        </div>
      </div>

      <div class="inner cover">

        <%= image_tag("slcrossbox", class:"img-responsive") %>

        <% if flash[:user_not_confirmed] %>
          <h1 class="cover-heading">Ups...</h1>
          <p class="lead">Parece que a tua conta ainda não foi confirmada pelo Bruno Sá</p>

        <% elsif flash[:user_created_account] %>
          <h1 class="cover-heading">Boa!</h1>
          <p class="lead">A tua conta foi criada e agora só precisas da confirmação do Bruno Sá</p>

        <% elsif flash[:email_already_exists] %>

          <h1 class="cover-heading">Ups...</h1>
          <p class="lead">Parece que já existe uma conta com esse email. Tens a certeza que já não estás inscrito?</p>

        <% else %>
          <h1 class="cover-heading">Treinar ou não treinar?</h1>
          <p class="lead">Treinar, claro! Vens hoje?</p>

          <div>
            <a href="/auth/facebook" class="btn btn-sm btn-facebook"><%= fa_stacked_icon "facebook", :base => "check-empty" %>Entrar com Facebook</a>
          </div>

          <div class="col-xs-10 col-xs-offset-1 col-sm-4 col-sm-offset-4">
            
            <form class="form" action="/signin" method="POST">

              <input type="hidden" value="<%= form_authenticity_token() %>" name="authenticity_token"/>
              <hr>
              <div class="form-group <% if flash[:wrong_credentials] %> has-error <% end %>">
                <div class="input-group">
                  <div class="input-group-addon"><%= fa_icon "envelope" %></div>
                  <label class="sr-only" for="input email">Email</label>
                  <input type="email" class="form-control" name="email" id="email" placeholder="Email">
                </div>
              </div>
              <div class="form-group <% if flash[:wrong_credentials] %> has-error <% end %>">
                <div class="input-group">
                  <div class="input-group-addon"><%= fa_icon "lock" %></div>
                  <label class="sr-only" for="password">Password</label>
                  <input type="password" class="form-control" name="password" id="password" placeholder="Password">
                </div>
                <% if flash[:wrong_credentials] %>
                <span id="password_help" class="help-block">Email não registado ou password errada. Tenta outra vez.</span>
                <% end %>
              </div>

              <button type="submit" class="btn btn-success btn-sm btn-block">Entrar</button>
              <p style="margin-top:15px;margin-bottom:30px;">Não tens conta? Cria uma <a href="/users/new">aqui</a>.</p>
              
            </form>

          </div>

        <% end %>

      </div>

      <div class="mastfoot">
        <div class="inner">
          <p>Built by <a href="https://www.facebook.com/miguelgazela">@miguelgazela</a></p>
        </div>
      </div>

    </div>

  </div>

</div>
